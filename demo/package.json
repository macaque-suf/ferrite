{
  "name": "wasm-audio-ferrite-demo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run copy:wasm && vite build",
    "copy:wasm": "mkdir -p public/wasm && cp -r ../dist/wasm/* public/wasm/",
    "preview": "vite preview"
  },
  "dependencies": {
    "wasm-audio-ferrite": "^0.1.2"
  },
  "devDependencies": {
    "@rollup/plugin-wasm": "^6.2.2",
    "vite": "^5.0.0"
  }
}
